{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/willow-memory/willow-mcp/docs/schema/handoff.schema.json",
  "title": "handoff.json (handoff_v1)",
  "description": "The structured record written by the handoff_write_v4 MCP tool at $WILLOW_HOME/dispatch/{dispatch_id}/handoff.json. closeout.md is RENDERED from this record (see handoff.py _render_closeout); this JSON is the source of truth. Grounded in src/willow_mcp/handoff.py.",
  "type": "object",
  "required": ["format", "dispatch_id", "app_id", "findings", "narrative", "written_at"],
  "properties": {
    "format": { "const": "handoff_v1" },
    "dispatch_id": { "type": "string", "minLength": 1 },
    "app_id": { "type": "string", "minLength": 1, "description": "Reporting agent (rendered as **From:** in closeout.md)." },
    "reply_to": { "type": "string", "default": "willow", "description": "Orchestrator (rendered as **To:**)." },
    "role": { "type": ["string", "null"] },
    "narrative": { "type": "string", "description": "Prose summary; rendered under '## What Was Done'." },
    "findings": {
      "type": "array",
      "description": "Rendered as the '## Findings' table (ID | Finding | Severity | Evidence).",
      "items": { "$ref": "#/$defs/finding" }
    },
    "checklist_resolved": { "type": "boolean", "description": "Drives the '## Checklist' checkbox." },
    "envelope_clean": { "type": "boolean" },
    "written_at": { "type": "string", "description": "UTC ISO timestamp (rendered as **Date:**)." }
  },
  "additionalProperties": true,
  "$defs": {
    "finding": {
      "type": "object",
      "required": ["id", "text", "severity"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "text": { "type": "string", "minLength": 1 },
        "severity": {
          "type": "string",
          "description": "Free string; verify_handoff reads it. Common vocabularies: high/medium/low (findings) — distinct from the mai @constraint set critical/error/warning/info."
        },
        "evidence": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Joined with ', ' into the Evidence column."
        }
      },
      "additionalProperties": true
    }
  }
}
